# Security: Prevent package-lock.json from being ignored
package-lock=true

# Security: Audit level
audit-level=moderate

# Security: Only allow HTTPS registry
registry=https://registry.npmjs.org/

# Performance: Use cache
cache=.npm

# Security: Verify package signatures
strict-ssl=true
